<UserControl x:Class="ArbDesk.Views.OpportunityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ArbDesk.ViewModels">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Master: Opportunity list -->
        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Opportunities}"
                  AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedOpportunity}"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="60" />
                <DataGridTextColumn Header="Symbol" Binding="{Binding CanonicalSymbol}" Width="80" />
                <DataGridTextColumn Header="Legs" Binding="{Binding LegsCompact}" Width="*" />
                <DataGridTextColumn Header="NetBps" Binding="{Binding NetBps}" Width="70" />
                <DataGridTextColumn Header="NetProfit" Binding="{Binding NetProfit}" Width="80" />
                <DataGridTextColumn Header="Annualized" Binding="{Binding AnnualizedNetBps}" Width="80" />
                <DataGridTextColumn Header="Conf" Binding="{Binding Confidence}" Width="50" />
                <DataGridTextColumn Header="Expires" Binding="{Binding ExpiresAt}" Width="80" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Detail + Confirm -->
        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="0,1,0,0" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="{Binding SelectedOpportunity.LegsCompact, FallbackValue='Select an opportunity'}"
                               FontSize="14" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding SelectedOpportunity.RejectReason}"
                               Foreground="Red" FontSize="11"
                               Visibility="{Binding SelectedOpportunity.Executable, Converter={x:Static vm:InverseBoolToVisibilityConverter.Instance}}" />
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Confirm"
                        Padding="20,8"
                        Command="{Binding ConfirmCommand}"
                        CommandParameter="{Binding SelectedOpportunity}"
                        IsEnabled="{Binding SelectedOpportunity.CanConfirm}"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
