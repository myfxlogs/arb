<UserControl x:Class="ArbDesk.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Filter + Load -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Strategy:" VerticalAlignment="Center" />
            <TextBox Text="{Binding StrategyFilter}" Width="120" Margin="4,0,16,0" />
            <Label Content="Limit:" VerticalAlignment="Center" />
            <TextBox Text="{Binding LimitText}" Width="60" Margin="4,0,16,0" />
            <Button Content="Load" Command="{Binding LoadCommand}" Padding="16,6" Margin="0,0,16,0" />
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#888" />
        </StackPanel>

        <!-- Signal History DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Signals}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="120" />
                <DataGridTextColumn Header="Strategy" Binding="{Binding Strategy}" Width="100" />
                <DataGridTextColumn Header="GrossBps" Binding="{Binding GrossBps}" Width="80" />
                <DataGridTextColumn Header="NetBps" Binding="{Binding NetBps}" Width="80" />
                <DataGridTextColumn Header="Executed" Binding="{Binding Executed}" Width="70" />
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
